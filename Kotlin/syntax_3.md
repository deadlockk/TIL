# Array

```
var intArray : Array<Int> = arrayOf(1,2,3)

var charArray : Array<Char> = arrayOf('A','B')

var stringArray : Array<String> = arrayOf("My,"Name")

var boolArray : Array<Boolean> = arrayOf(true, false)

println(intArray.toList())

```
